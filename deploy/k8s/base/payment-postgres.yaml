apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: payment-postgres-data
  namespace: ticketportal
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-postgres
  namespace: ticketportal
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-postgres
  template:
    metadata:
      labels:
        app: payment-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:17-alpine
          env:
            - name: POSTGRES_USER
              value: payment
            - name: POSTGRES_PASSWORD
              value: payment
            - name: POSTGRES_DB
              value: payment
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: payment-postgres-data
---
apiVersion: v1
kind: Service
metadata:
  name: payment-postgres
  namespace: ticketportal
spec:
  selector:
    app: payment-postgres
  ports:
    - port: 5432
      targetPort: 5432
