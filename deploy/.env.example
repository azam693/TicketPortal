# Скопируй в deploy/.env и при желании поменяй значения.
# Эти же креды должны совпадать с ConnectionStrings в appsettings.Development.json.
CATALOG_DB_USER=catalog
CATALOG_DB_PASSWORD=catalog
CATALOG_DB_NAME=catalog
